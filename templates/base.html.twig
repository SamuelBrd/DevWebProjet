<!DOCTYPE html>
<html>
<head>
    <title>{% block title %}{% trans %} Organization of various events {% endtrans %}{% endblock %}</title>

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>

    {% block stylesheets %}{% endblock %}

    {% block javascripts %}{% endblock %}
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>
<body>
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">

            <a class="navbar-brand" href="/{_locale}/"><span class="glyphicon glyphicon-home"></span></a>
            <a class="navbar-brand" href="/{_locale}/event">{% trans %} Events {% endtrans %}</a>
            <a class="navbar-brand" href="/{_locale}/commu">{% trans %} Community {% endtrans %}</a>
        </div>

        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <div>
                        {% if not is_granted('ROLE_USER') %}
                            <a class="navbar-brand" href="/{_locale}/login"><span class="glyphicon glyphicon-off" aria-hidden="true"></span></a>
                            <a class="navbar-brand" href="/{_locale}/register">{% trans %} Registration {% endtrans %}</a>
                        {% else %}
                            <a class="navbar-brand" href="/{_locale}/logout"><span class="glyphicon glyphicon-off" aria-hidden="true"></span></a>
                        {% endif %}
                        {% for key,locale in locales %}
                            <li><a href="{{ path(app.request.get('_route'),
                            app.request.attributes.get('_route_params')|merge({'_locale': key})) }}">{{locale}}</a></li>
                        {% endfor %} 
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    {% block body %}{% endblock %}
</div>
</body>
</html>
